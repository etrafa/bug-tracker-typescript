export const a = "Laura";

// import {
//   collection,
//   deleteDoc,
//   getDocs,
//   query,
//   where,
// } from "firebase/firestore";
// import { db } from "../firebaseConfig";

// export const deleteProject = async (
//   id: string,
//   userList: [],
//   projectName: string
// ) => {
//   await userList.forEach(async (user: {} | undefined) => {
//     const colRef = query(collection(db, "users", user?.id, "tickets"));
//   });
// };
